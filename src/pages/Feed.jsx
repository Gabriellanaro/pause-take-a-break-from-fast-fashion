/*
  Feed.jsx
  ─────────
  Main page showing all events in a scrollable feed.
  Fetches data from events.json and displays EventCard components (coming soon).
*/

function Feed() {
  return (
    <section className="space-y-4">
      <h2 className="text-lg font-medium mb-2">Events Feed</h2>
      <p className="text-gray-600">
        This is the feed page. Soon it will list all events from the JSON file.
      </p>
    </section>
  );
}

export default Feed;
